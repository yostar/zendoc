# Workspace Instructions

This is my writing workspace. When I'm working inside a project folder (e.g., `projects/coach/`), read that project's AGENTS.md for project-specific instructions.

---

## Zendoc Project Context (do not forget)

**This repo is the prototype** where everything was created: all extensions installed and configured, the Zendoc extension developed and tested. The `extension/` folder contains the published Zendoc for Cursor extension (Open VSX). This workspace is the development environment—not a user-created Zendoc workspace. 